`com.agentflow` 是整个后端的**根包**。因为 [AgentFlowApplication.java](/Users/xavier/AgentFlow-Hub/backend/src/main/java/com/agentflow/AgentFlowApplication.java) 上有 `@SpringBootApplication`，Spring Boot 默认会扫描它所在包以及所有子包，所以放在 `com.agentflow` 下面的 Controller、Service、配置类都会被自动发现。

这个目录结构的核心思路是：**按业务模块拆包，而不是全局堆 controller/service/mapper。**

```
com.agentflow
  common      通用基础能力
  config      Spring 配置
  user        用户和登录
  knowledge   知识库
  rag         检索增强生成
  agent       Agent 配置和执行引擎
  tool        工具系统
  task        任务和 SSE
  trace       执行轨迹
  harness     Agent 运行治理
  evaluation  评测
  demo        模拟业务数据
  infra       外部系统适配
```

各目录未来大概这样放：

| 目录         | 未来放什么                                                   |
| ------------ | ------------------------------------------------------------ |
| `common`     | 全项目通用代码，比如 `ApiResponse`、统一异常、错误码、分页对象、traceId filter、当前用户解析器 |
| `config`     | Spring 配置类，比如 Security、Redis、MyBatis-Plus、Spring AI、SSE、跨域配置 |
| `user`       | 用户注册、登录、JWT、当前用户、角色权限                      |
| `knowledge`  | 知识库 CRUD、文档上传、文档解析、chunk 切分、文档状态        |
| `rag`        | 向量检索、召回结果组装、RAG prompt、引用溯源、检索测试接口   |
| `agent`      | Agent 创建、Agent 配置、system prompt、AgentEngine、执行状态机 |
| `tool`       | 工具定义、工具绑定、ToolRuntime、内置工具，比如订单查询、支付日志查询 |
| `task`       | Agent 任务创建、任务状态、任务事件、SSE 推送、取消任务       |
| `trace`      | LLM 调用日志、RAG 命中日志、工具调用日志、Agent step、trace 查询接口 |
| `harness`    | PolicyGuard、Episode Package、运行证据包导出、后续评测复盘支撑 |
| `evaluation` | 评测集、评测 case、评测运行、结果统计                        |
| `demo`       | 模拟订单、支付日志、工单数据，以及临时 demo 接口             |
| `infra`      | 对外部依赖的封装，比如 LLM、embedding、Qdrant、PostgreSQL、Redis、MinIO、RabbitMQ |

以后每个业务模块内部通常再细分：

```
controller   接 HTTP 请求
service      写业务流程
repository   查数据库
model        数据库实体/领域对象
dto          请求和响应对象
```

比如以后 `knowledge` 可能长这样：

```
knowledge
  controller
    KnowledgeBaseController.java
  service
    KnowledgeBaseService.java
  repository
    KnowledgeBaseMapper.java
  model
    KnowledgeBase.java
  dto
    CreateKnowledgeBaseRequest.java
    KnowledgeBaseResponse.java
  parser
    MarkdownDocumentParser.java
  chunk
    TextChunker.java
```

你现在已经写的 [HealthController.java](/Users/xavier/AgentFlow-Hub/backend/src/main/java/com/agentflow/common/web/HealthController.java) 放在 `common/web`，是因为健康检查属于全局基础接口，不属于某个业务模块；[HelloController.java](/Users/xavier/AgentFlow-Hub/backend/src/main/java/com/agentflow/demo/controller/HelloController.java) 放在 `demo/controller`，是因为它只是学习和启动验证用的临时 demo。

一个好记的判断方式是：
**多个模块都会用的，放 `common`；连接外部系统的，放 `infra`；具体业务自己的，放对应业务包。**