package com.agentflow.knowledge.readiness;

/** Derived retrieval eligibility, never a second writable document state. */
public enum RetrievalReadiness {
    NOT_READY, INDEXING, READY, DEGRADED, FAILED
}
