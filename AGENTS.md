# AgentFlow Hub 项目约定

## `slice-docs/` 下切片说明文档的面试问答

- 本规则仅适用于仓库根目录 `slice-docs/` 目录下以两位数字开头的
  `*_PACKAGE_INTERFACE.md` 切片说明文档。
- 新增、修改或补全此类文档时，保留原有的目标、接口、数据契约、
  实现/验收和“明确不做”边界；在文末增加 `## 面试问题与回答`。
- 该节给出 3–6 个与本切片实际代码、契约、测试或明确设计决策直接
  相关的高频问题；每题以“问题 / 回答”成对书写。回答应说明设计选择、
  原因、取舍或失败边界，并可指向具体接口、状态、字段或验收证据。
- 只写本切片能够证明或已明确约定的内容。未实现或后续切片能力必须
  明确标为“未纳入本切片/后续规划”，不得写成已完成能力；不写脱离
  切片的通用题库，也不因此扩大实现范围。
- 必须区分已实现行为、单元测试或本地/mock 验收、真实外部服务验收与
  后续规划；不得把本地 adapter、mock 结果或设计契约表述为线上能力。
- 切片范围、代码或验收证据变化时，同步更新对应问答，避免保留过时的
  面试表述。
